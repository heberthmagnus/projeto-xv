import { requireAdmin } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
import { updateSuggestion } from "./actions";
export default async function SuggestionsPage(){await requireAdmin();const suggestions=await prisma.siteSuggestion.findMany({orderBy:{createdAt:"desc"}});return <main className="xv-page-shell"><div className="xv-page-container"><section className="xv-card"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#8B6914]">Fale conosco</p><h1 className="mt-1 text-2xl font-black">Sugestões recebidas</h1><div className="mt-5 grid gap-4">{suggestions.length?suggestions.map(item=><article key={item.id} className="rounded-xl border p-4"><div className="flex flex-wrap justify-between gap-2"><b>{item.subject}</b><span className="text-sm text-[#6B7280]">{item.createdAt.toLocaleDateString("pt-BR")}</span></div><p className="mt-2 whitespace-pre-wrap">{item.message}</p><p className="mt-2 text-sm text-[#6B7280]">{item.name||"Anônimo"}{item.contact?` · ${item.contact}`:""}{item.allowContact?" · aceita contato":""}</p><form action={updateSuggestion} className="mt-4 grid gap-2 md:grid-cols-[180px_1fr_auto]"><input type="hidden" name="id" value={item.id}/><select name="status" defaultValue={item.status} className="rounded-lg border p-2"><option>NOVO</option><option>EM_ANALISE</option><option>EM_ANDAMENTO</option><option>RESOLVIDO</option></select><input name="adminNotes" defaultValue={item.adminNotes||""} placeholder="Observação interna" className="rounded-lg border p-2"/><button className="rounded-lg bg-[#101010] px-3 py-2 font-bold text-white">Salvar</button></form></article>):<p className="text-[#6B7280]">Nenhuma sugestão recebida ainda.</p>}</div></section></div></main>}
